<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
  <link rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css" />
  <style>
    .product-card {
      margin-bottom: 20px;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>Lista de Productos</h1>

    {{#if products.length}}
    <div class="row">
      {{#each products}}
      <div class="col s12 m6 l4">
        <div class="card small product-card">
          <div class="card-title center-align bold">
            <h4>{{this.title}}</h4>
          </div>
          <div class="card-content">
            <p class="content-padding">Precio: ${{this.price}}</p>
            <p class="content-padding">Categoría: {{this.category}}</p>
            <p class="content-padding">Stock: {{this.stock}}</p>
            <p class="content-padding">{{this.description}}</p>
          </div>
          <div class="card-action center-align">
            <a href="#" class="btn btn-custom hoverable">Agregar al carrito</a>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
    {{else}}
    <p>No hay productos disponibles.</p>
    {{/if}}
  </div>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>

</html>